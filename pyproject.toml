[project]
name = "training-backend"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic-settings",
]

[tool.setuptools]
packages = ["app"]

[tool.ruff]
line-length = 88