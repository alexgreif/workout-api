[project]
name = "training-backend"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic-settings",
    "sqlalchemy>=2.0",
    "psycopg[binary]",
    "pydantic[email]",
    "alembic",
]

[tool.setuptools]
packages = ["app"]

[tool.ruff]
line-length = 88